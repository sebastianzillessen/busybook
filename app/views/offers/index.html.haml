%h1 Listing offers

%table
  %thead
    %tr
      %th Subject
      %th Email
      %th Time Slots?
      %th(colspan="3" )
        Actions

  %tbody
    - @offers.where(approved: false).each do |offer|
      %tr
        %td= offer.description
        %td= offer.email
        %td= offer.offered_schedules.count
        %td= link_to 'Show', offer
        %td= link_to 'Edit', edit_offer_path(offer)
        %td= link_to 'Destroy', offer, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Offer', new_offer_path
